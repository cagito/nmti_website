# Exported rework prompts (generated)

`npm run rework:export-prompts` 로 생성. Git에는 커밋하지 않음.

| 파일 | 내용 |
|------|------|
| `IMG-###.txt` | P0 + Figure 복붙 블록 |
| `_INDEX.txt` | ID · 제목 목록 |

재생성: `npm run rework:export-prompts` · Phase만: `-- --phase A`
