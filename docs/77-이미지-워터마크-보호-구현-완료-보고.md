# 이미지 워터마크·보호 — 구현 완료 보고

**완료일:** 2026-06-22  
**정본 계획:** [76-이미지-워터마크-보호-통합-계획](./76-이미지-워터마크-보호-통합-계획.md)

---

## 요약

WATERMARK-01을 **1차(픽셀 합성) + 2차(클라이언트 억제)** 로 적용했습니다.

| 계층 | 내용 | 상태 |
|------|------|------|
| 1차 합성 | IMG-001~110 PNG 우하단 NMTI 로고 | ✅ 110종 |
| WebP | `convert-technology-webp.py --force` | ✅ 110종 |
| 2차 JS | `js/image-protection.js` — 우클릭·드래그·복사 억제 | ✅ |
| 라이트박스 | `figure-lightbox.js` 확대 이미지 동일 보호 | ✅ |
| SPA | `content-loader.js` · `app.js` | ✅ |
| SEO | `generate-technology-seo-pages.mjs` (121페이지 재생성) | ✅ |
| 홈 P1 | `index.html` `main2.png` · `smartPhoneAlert.png` | ✅ |
| 검증 | `npm run audit:figure-watermark` | ✅ 110/110 |

---

## 신규·변경 파일

| 경로 | 역할 |
|------|------|
| `assets/images/brand/nmti-watermark.svg` | 워터마크 벡터 원본 |
| `assets/images/brand/nmti-watermark.png` | 합성·CSS 백업용 래스터 |
| `scripts/lib/watermark_stamp.py` | `stamp_watermark()` · `ensure_watermark_asset()` |
| `scripts/apply-figure-watermark.py` | PNG 일괄 스탬프 |
| `scripts/watermark-manifest.json` | SHA-256 + `watermarked: true` |
| `scripts/audit-figure-watermark.mjs` | manifest 커버리지 검증 |
| `js/image-protection.js` | 클라이언트 보호 모듈 |
| `package.json` | `watermark:figures` · `audit:figure-watermark` |

**캐시 버전:** `technology.css?v=8` · `app.js?v=13`

---

## 재생성 순서 (운영)

```bash
npm run watermark:figures
python scripts/convert-technology-webp.py --force
node scripts/generate-image-assets.mjs
npm run build:content && npm run build:seo
npm run audit:figure-watermark
```

---

## 검증 결과

- `audit:figure-watermark`: **PASS** (110 PNG)
- `verify:local`: **기존** `requiresReaudit` 10건(IMG-008, 009, 015, …)으로 중단 — 워터마크 작업과 무관한 선행 검수 이슈

---

## 한계 (계획서 §0 동일)

DevTools·직링크·스크린샷으로는 완전 차단 불가. 목표는 **출처 표시** + **일반 사용자 우클릭·드래그 억제**입니다.
